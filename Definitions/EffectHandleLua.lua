--- @class EffectHandleLua 
EffectHandleLua = {}

--- Stop: Stop emission immediately; live particles fade out naturally.
--- @return nil
function EffectHandleLua:Stop()
	-- dummy implementation for documentation purposes only
end

--- Position: Move the effect to a new location.
--- @param pos Loc
function EffectHandleLua:Position(pos)
	-- dummy implementation for documentation purposes only
end

--- Scale: Resize the effect.
--- @param scale number
--- @return nil
function EffectHandleLua:Scale(scale)
	-- dummy implementation for documentation purposes only
end

--- Rotate: Rotate the effect, in degrees. Sets the effect's local rotation about the Z axis (an in-plane spin on the top-down map -- useful for aiming a directional effect at a target). Replaces any prior rotation rather than accumulating.
--- @param degrees number
--- @return nil
function EffectHandleLua:Rotate(degrees)
	-- dummy implementation for documentation purposes only
end
